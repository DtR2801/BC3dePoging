<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Barroc_intens.Pages.CreateAppointmentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Barroc_intens.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>

        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" 
                x:Name="MainCreateFormStackPanel" 
                Margin="65,8.5"
                HorizontalAlignment="Left">
            <TextBlock FontSize="28" Text="Nieuwe Afspraak" Margin="5, 10, 0, 45"/>

            
        </StackPanel>

        <StackPanel x:Name="buttonStackPanel" 
    Orientation="Vertical"
    VerticalAlignment="Bottom"
    Grid.Column="1">
            <Button x:Name="SaveButton" 
        Click="SaveButton_Click"
        Background="#FACA16"
        Foreground="Black"
        FontSize="18"
        Height="45"
        Width="45"    
        Margin="5,5,10,5">
                <Button.Content>
                    <SymbolIcon Symbol="Save"/>
                </Button.Content>
            </Button>
            <Button x:Name="cancelButton" 
        Click="cancelButton_Click"
        Background="#FE6F5E"
        Foreground="Black"
        FontSize="18"
        Height="45"
        Width="45"    
        Margin="5,5,10,10">
                <Button.Content>
                    <SymbolIcon Symbol="Cancel"/>
                </Button.Content>
            </Button>
        </StackPanel>
    </Grid>
</Page>
